**Fab** — the accent circle pinned bottom-right; the persistent help "?" on every screen.

```jsx
<Fab icon="?" title="Bantuan" onClick={openHelp} />
```

- Fixed-position; renders relative to the viewport. One per screen.
