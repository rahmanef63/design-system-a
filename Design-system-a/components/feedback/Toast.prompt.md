**Toast** — the ink snackbar confirming an action (starred, duplicated, moved). Icon + message + optional inline action. It's presentational — the host decides when to show/dismiss.

```jsx
<Toast icon="★" message="Ditambahkan ke berbintang" action="Urungkan" />
```
