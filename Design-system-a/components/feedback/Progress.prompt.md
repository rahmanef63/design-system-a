**Progress** — loading state. `skeleton` (default) is a stack of hatched placeholder bars matching the "art not ready" language; `bar` is a determinate accent track.

```jsx
<Progress variant="skeleton" lines={3} />
<Progress variant="bar" value={65} />
```
