**Backdrop** — the scrim that dims the page behind a modal or drawer and dismisses on click. Composed automatically by ModalDialog/Drawer; use it directly only for a custom overlay.

```jsx
<Backdrop onClick={close} />
```
