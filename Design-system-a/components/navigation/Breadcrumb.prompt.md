**Breadcrumb** — a back arrow + path, used atop context sidebars (Brand Kit: "◂ Semua Template Merek").

```jsx
<Breadcrumb path={["Semua Template Merek"]} onBack={goUp} />
```

- The last segment is inked as current; earlier ones are secondary.
