**Tabs** — underlined segmented nav for switching sub-sections. The system defaults to filter chips for most switching; reach for Tabs only when a true tabbed section is warranted.

```jsx
<Tabs tabs={[{key:"all",label:"Semua"},{key:"mine",label:"Milik saya"}]} value={tab} onChange={setTab} />
```
