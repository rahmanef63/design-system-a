**Avatar** — initials in a circle (`user`, accent fill) or rounded square (`team`, neutral). Mono type, sized by `size`.

```jsx
<Avatar initials="RF" />
<Avatar initials="RF" variant="team" size={34} />
```
