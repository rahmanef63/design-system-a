**MainRegion** — the full-height scrolling area that holds a page's HeroHeader + ContentSections. One per screen; the FAB positions against it.

```jsx
<MainRegion>
  {/* HeroHeader, sections, tables… */}
</MainRegion>
```
